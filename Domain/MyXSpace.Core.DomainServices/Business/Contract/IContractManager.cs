using System;
using System.Collections.Generic;
using MyXSpace.Core.Entities;

namespace MyXSpace.Core.Services
{
    /// <summary>
    /// Interface pour les services de gestion des contrats.
    /// </summary>
    public interface IContractManager
    {
        //    void SetContractAsSigned(Contract contract, ContractOrigin? signOrigin, DateTime? signingDate = null);

        //    void SetContractAsUnSigned(Contract contract);

        //    int ShiftSignedContracts();

        //    bool SignContract(Contract contract, ContractOrigin? signOrigin, DateTime? signingDate = null);

        //    SignatureStatus SignContractCertEurope(User user, string toSignContent);

        //    IEnumerable<Contract> GetContractsByUser(User user);
        //}
    }
}